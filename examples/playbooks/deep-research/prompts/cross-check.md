Cross-check these research claims. Group claims that assert the same fact across different source URLs. Keep a claim only when it is supported by at least {{minSupport}} distinct URLs OR by one clearly authoritative source. Discard single-source weak claims and flag conflicts instead of picking a side.

Claims:
{{claims}}

Return one kept claim per line as `KEEP: <claim> — <url1> | <url2>`, and one per discarded claim as `DROP: <claim>`.
