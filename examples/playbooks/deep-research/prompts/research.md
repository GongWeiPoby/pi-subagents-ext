Research this angle of the question using web search and fetching:

Question: {{question}}
Angle: {{angle}}

Search first, then read the most relevant results. Return concrete, verifiable claims, each tagged with the exact source URL it came from — one per line, `CLAIM: <url> — <claim>`. Report only what the fetched pages actually say; never invent a source. Say `CLAIMS: none` when the angle turns up nothing.
