Verify this audit finding against the actual code with read/grep before it reaches the report:

{{finding}}

Return whether the finding is confirmed, corrected (with the correction), or a false positive (with why). A finding without a location you actually read is unverified, not confirmed.
