/** Shared loading animation used by agent and task status surfaces. */

/** Braille frames for active execution rows. Keep every frame one cell wide. */
export const BRAILLE_SPINNER_FRAMES = ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"] as const;

/** Cadence for execution-row animation. */
export const SPINNER_INTERVAL_MS = 80;
